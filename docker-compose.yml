version: '3.8'

services:
  app:
    build:
      context: ./Back
      dockerfile: Dockerfile
    container_name: sitio_parroquial_app
    restart: always
    ports:
      - "3000:3000"
    volumes:
      - ./Back:/app
    environment:
      - MONGO_URI=${MONGO_URI}
      - JWT_SECRET=${JWT_SECRET}

  db:
    image: mongo:latest
    container_name: sitio_parroquial_db
    restart: always
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data: